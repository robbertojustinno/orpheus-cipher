import type{ActivationResult,LicenseIdentity,LicenseProvider,PersistedLicenseState}from'../types/license';import{ProductionLicenseProvider}from'./productionLicenseProvider'
const DEV_LICENSE:LicenseIdentity={licenseId:'LIC-DEV-MASTER',type:'MASTER',issuedAt:'2026-01-01T00:00:00Z',status:'active',capabilities:['orpheus-core','narrative','osint','enigma-engine','master-reset','release-validation','developer-tools'],keyId:'development-only',maxActivations:9999,offlineGraceDays:365}
export class DevelopmentLicenseProvider implements LicenseProvider{private production=new ProductionLicenseProvider();async activate(key:string,installationId:string):Promise<ActivationResult>{if(key!=='DEV-MASTER')return this.production.activate(key,installationId);const now=new Date().toISOString(),state:PersistedLicenseState={version:1,installationId,license:DEV_LICENSE,token:'development-session',activatedAt:now,lastValidatedAt:now};return{ok:true,code:'ACCEPTED',license:DEV_LICENSE,state}}async validate(state:PersistedLicenseState){if(state.license.licenseId!==DEV_LICENSE.licenseId)return this.production.validate(state);return{valid:true,license:DEV_LICENSE,code:'VALID',offline:true,graceRemainingDays:365}}}
