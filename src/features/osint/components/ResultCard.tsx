import type{OsintResult}from'../types/osint';import styles from'./DeepSearchPanel.module.css';export function ResultCard({result,onOpen}:{result:OsintResult;onOpen:(url:string)=>void}){return <article className={styles.resultCard}><div><small>{result.source} // {result.resultType}</small>{import.meta.env.DEV&&result.devData&&<b>SIMULATED DATA</b>}</div><h3>{result.title}</h3><p>{result.snippet}</p><code>{result.url}</code><footer><span>CONFIDENCE {result.confidence===undefined?'N/A':Math.round(result.confidence*100)+'%'}</span><button onClick={()=>onOpen(result.url)}>ABRIR NO NAVEGADOR</button></footer></article>}
