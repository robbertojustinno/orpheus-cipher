import type{SearchSession}from'../types/osint';import{exportSessionCsv,exportSessionJson}from'../engine/exportEngine';import{createPreliminaryReport}from'../engine/reportEngine';import styles from'./DeepSearchPanel.module.css'
const download=(name:string,content:string,type:string)=>{const blob=new Blob([content],{type});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=name;link.click();URL.revokeObjectURL(url)}
export function downloadSession(session:SearchSession,format:'json'|'csv'){download(`orpheus-${session.id}.${format}`,format==='json'?exportSessionJson(session):exportSessionCsv(session),format==='json'?'application/json':'text/csv')}
export function ExportPanel({session}:{session:SearchSession}){return <section className={styles.export}><button onClick={()=>downloadSession(session,'json')}>EXPORT JSON</button><button onClick={()=>downloadSession(session,'csv')}>EXPORT CSV</button><details><summary>PRELIMINARY REPORT</summary><pre>{createPreliminaryReport(session)}</pre></details></section>}
