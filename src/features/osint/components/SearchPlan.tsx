import type{SearchQuery}from'../types/osint';import styles from'./DeepSearchPanel.module.css';export function SearchPlan({plan,onOpen}:{plan:SearchQuery[];onOpen?:(query:string)=>void}){return <section className={styles.block}><header>SEARCH PLAN <b>{plan.length} QUERIES</b></header>{plan.map(item=><div className={styles.plan} key={item.id}><small>{item.strategy}</small><code>{item.value}</code>{onOpen&&<button onClick={()=>onOpen(item.value)}>ABRIR</button>}</div>)}</section>}
