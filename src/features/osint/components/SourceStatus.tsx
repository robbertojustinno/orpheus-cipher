import type{ProviderRunStatus}from'../types/osint';import styles from'./DeepSearchPanel.module.css';export function SourceStatus({items}:{items:ProviderRunStatus[]}){return <section className={styles.block}><header>SOURCES</header>{items.map(item=><div className={styles.source} key={item.id}><span>{item.name}</span><i>{'.'.repeat(Math.max(3,22-item.name.length))}</i><strong data-status={item.status}>{item.status.toUpperCase()} {item.resultCount?`// ${item.resultCount}`:''}</strong></div>)}</section>}
