import type{SearchTarget,SearchTargetType}from'../types/osint'
export const normalizeQuery=(input:string)=>input.normalize('NFKC').trim().replace(/\s+/g,' ')
export function detectTarget(input:string):SearchTarget{const normalized=normalizeQuery(input);let type:SearchTargetType='keyword';let alternatives:SearchTargetType[]|undefined;if(/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalized))type='email';else if(/^@[A-Za-z0-9_.-]+$/.test(normalized))type='username';else if(/^(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z]{2,}$/i.test(normalized))type='domain';else if(/\.(pdf|docx?|xlsx?|pptx?)$/i.test(normalized))type='document';else if(/\b(ltda|s\.?a\.?|inc\.?|corp\.?|company|industries|empresa|instituto|fundação|organização)\b/i.test(normalized))type='organization';else if(normalized.split(' ').length>=2){type='person';alternatives=['keyword']}return{raw:input,normalized,type,alternatives}}
