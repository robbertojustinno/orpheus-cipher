import type{SearchMode,SearchQuery,SearchTarget}from'../types/osint'
const query=(value:string,strategy:string,index:number):SearchQuery=>({id:`q-${index+1}`,value,strategy})
export function createSearchPlan(target:SearchTarget,mode:SearchMode){const exact=`"${target.normalized.replace(/"/g,'')}"`;const values=mode==='quick'?[exact]:mode==='dork'?[target.normalized]:[exact,`${exact} filetype:pdf`,`${exact} filetype:docx OR filetype:xlsx OR filetype:pptx`,`${exact} site:linkedin.com`,`${exact} relatório OR report`,`${exact} contato OR contact`,`${exact} publicação OR publication`,`${exact} referência OR reference`];if(target.type==='domain')values.push(`site:${target.normalized}`,`"${target.normalized}" nameserver OR DNS`);if(target.type==='username')values.push(`"${target.normalized.replace(/^@/,'')}" perfil OR profile`);return[...new Set(values)].map((value,index)=>query(value,index===0?'GENERAL WEB':value.includes('filetype:')?'DOCUMENTS':value.startsWith('site:')?'DOMAINS':'MENTIONS',index))}
