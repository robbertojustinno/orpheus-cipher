import type{DorkFields}from'../types/osint'
const clean=(value='')=>value.trim().replace(/[\r\n]/g,' ')
export function buildDork(fields:DorkFields){const parts:string[]=[];if(clean(fields.domain))parts.push(`site:${clean(fields.domain).replace(/^https?:\/\//,'').replace(/\/.*$/,'')}`);if(clean(fields.filetype))parts.push(`filetype:${clean(fields.filetype).replace(/^\./,'')}`);if(clean(fields.title))parts.push(`intitle:"${clean(fields.title).replace(/"/g,'')}"`);if(clean(fields.url))parts.push(`inurl:"${clean(fields.url).replace(/"/g,'')}"`);if(clean(fields.term))parts.push(`"${clean(fields.term).replace(/"/g,'')}"`);if(clean(fields.exclude))parts.push(...clean(fields.exclude).split(/\s+/).map(value=>`-${value}`));return parts.join(' ')}
const blocked=[/\b(passwords?|senhas?|credentials?|credenciais|tokens?|private[ -]?keys?|chaves? privadas?)\b/i,/\b(shells?|webcams?|admin panels?|pain[eé]is? vulner[aá]veis?)\b/i,/\b(config|env)\b.*\b(secret|password|token)\b/i]
export function validateDork(query:string){const normalized=clean(query);if(!normalized)return{allowed:false,reason:'EMPTY QUERY'};if(blocked.some(pattern=>pattern.test(normalized)))return{allowed:false,reason:'QUERY BLOCKED: reformule para uma finalidade pública e legítima.'};return{allowed:true}}
export const PUBLIC_DORK_LIBRARY=[{category:'DOCUMENTOS PÚBLICOS',template:'filetype:pdf "termo"'},{category:'MENÇÕES',template:'"termo" -site:example.com'},{category:'DOMÍNIOS',template:'site:example.com "termo"'},{category:'ARQUIVOS INSTITUCIONAIS',template:'site:gov.br filetype:pdf "termo"'},{category:'PERFIS PÚBLICOS',template:'site:linkedin.com "nome"'},{category:'PUBLICAÇÕES',template:'intitle:"publicação" "termo"'},{category:'RELATÓRIOS',template:'filetype:pdf intitle:"relatório" "termo"'},{category:'REFERÊNCIAS',template:'intext:"termo" OR intext:"referência"'}] as const
