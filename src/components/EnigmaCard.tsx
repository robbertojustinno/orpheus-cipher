import type{Enigma}from'../types';import{Icon}from'./Icon';import styles from'./EnigmaCard.module.css'
const statusLabel={locked:'bloqueado',available:'disponível',active:'em análise',solved:'concluído'} as const
export function EnigmaCard({enigma,onAction}:{enigma:Enigma;onAction:(e:Enigma)=>void}){const locked=enigma.status==='locked';const action=locked?'Bloqueado':enigma.status==='solved'?'Abrir':enigma.status==='active'?'Analisar':'Acessar';return <article className={`${styles.card} ${locked?styles.locked:''}`}><div className={styles.top}><span>CAIXA // {String(enigma.number).padStart(2,'0')}</span><i>{statusLabel[enigma.status]}</i></div><div className={styles.visual}><span>{String(enigma.number).padStart(2,'0')}</span><div className={styles.cube}><Icon name={locked?'lock':'box'} size={24}/></div>{locked&&<b><Icon name="lock" size={10}/> RESTRITO</b>}</div><div className={styles.body}><h3>{enigma.title}</h3><p>{enigma.description}</p><div className={styles.meta}><span>PROGRESSO</span><strong>{enigma.progress}%</strong></div><div className={styles.progress}><i style={{width:`${enigma.progress}%`}}/></div><button disabled={locked} onClick={()=>onAction(enigma)}>{locked&&<Icon name="lock" size={13}/>} {action} {!locked&&<Icon name="arrow" size={14}/>}</button></div></article>}
