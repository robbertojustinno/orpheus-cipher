import{useEffect,useState}from'react';import type{OperatorIdentity}from'../types';import{Icon}from'./Icon';import styles from'./TopBar.module.css'
export function TopBar({onMenu,identity,founderAccess}:{onMenu:()=>void;identity:OperatorIdentity;founderAccess:string}){const[now,setNow]=useState(new Date());useEffect(()=>{const t=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(t)},[]);const time=now.toLocaleTimeString('pt-BR');const date=now.toLocaleDateString('pt-BR',{day:'2-digit',month:'short',year:'numeric'}).replace('.','').toUpperCase();const initials=identity.username==='OPERADOR'?'OP':identity.username.slice(0,2).toUpperCase();return <header className={styles.topbar}><button className={styles.menu} onClick={onMenu}><Icon name="menu"/></button><div className={styles.mark}>O<i/></div><div className={styles.brand}><strong>ORPHEUS</strong><small>SISTEMA DE INTELIGÊNCIA <em>//</em> ACESSO CIPHER</small></div><div className={styles.founders}><span>FOUNDERS ACCESS</span><strong>{founderAccess}</strong></div><div className={styles.statuses}><div><i className={styles.red}/><small>NÍVEL DE AMEAÇA</small><strong>ELEVADO</strong></div><div><i className={styles.green}/><small>SESSÃO SEGURA</small><strong>ATIVA</strong></div><div><i/><small>DETECÇÃO ATIVA</small><strong>ONLINE</strong></div></div><div className={styles.clock}><strong>{time}</strong><small>{date}</small></div><div className={styles.operator}><div className={styles.avatar}>{initials}<i/></div><div><strong>{identity.username}</strong><small>OPERADOR</small></div></div></header>}
