export class ResendEmailProvider{constructor(config){this.config=config}async notifyPhysical(claim){const c=this.config;if(!c.resendApiKey||!c.adminEmail||!c.emailFrom)throw new Error('EMAIL_PROVIDER_NOT_CONFIGURED');const address=[claim.address,`${claim.address_number}${claim.address_complement?` / ${claim.address_complement}`:''}`].join('\n');const text=`NOVO RESGATE CIPHER\n\nOperador:\n${claim.display_name}\n\nLicença:\n${claim.license_label}\n\nCódigo:\n${claim.verification_code}\n\nConcluído:\n${claim.completed_at}\n\nSolicitado:\n${claim.claimed_at}\n\nNome para envio:\n${claim.full_name}\n\nEndereço:\n${address}\n\nCEP:\n${claim.postal_code}\n\nCidade/Estado:\n${claim.city}/${claim.state}\n\nPaís:\n${claim.country}\n\nTelefone:\n${claim.phone}\n\nE-mail:\n${claim.email}\n\nPainel:\n${c.adminBaseUrl}/admin/rewards`;const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${c.resendApiKey}`,'content-type':'application/json'},body:JSON.stringify({from:c.emailFrom,to:[c.adminEmail],subject:`CIPHER // NOVO RESGATE FÍSICO — ${claim.license_label}`,text})});if(!response.ok)throw new Error(`EMAIL_PROVIDER_HTTP_${response.status}`)}}
