import{describe,expect,it}from'vitest';import{enrichWithDns}from'../src/features/investigation/engine/domainEnrichment';import{createInvestigation}from'../src/features/investigation/engine/investigationEngine';import type{SearchSession}from'../src/features/osint/types/osint'
const session:SearchSession={id:'dns-session',query:'example.com',targetType:'domain',mode:'deep',createdAt:'2026-01-01T00:00:00.000Z',privateMode:false,plan:[],providers:[],correlations:[],results:[]}
describe('public DNS enrichment',()=>{it('adds authoritative DNS entities, evidence and explainable graph relationships',()=>{const base=createInvestigation(session);const enriched=enrichWithDns(base,{domain:'example.com',a:['93.184.216.34'],aaaa:[],mx:['10 mail.example.com.'],ns:['a.iana-servers.net.'],cname:[],txt:['v=spf1 -all'],queriedAt:'2026-01-01T01:00:00.000Z',status:'ONLINE'});expect(enriched.entities.some(entity=>entity.normalizedValue==='a.iana-servers.net')).toBe(true);expect(enriched.evidence.some(evidence=>evidence.quality==='authoritative')).toBe(true);expect(enriched.relationships.length).toBeGreaterThan(0);expect(enriched.relationships.some(link=>link.explanation.includes('PUBLIC DNS'))).toBe(true)})})
